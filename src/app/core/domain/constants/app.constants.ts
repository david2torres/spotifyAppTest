export const APP_CONSTANTS = {
    session: 'spotifyToken',
    access: 'access_token',
}

export const REQUEST_CONST = {
    id: 'id'
}

export const URL_CONST = {
    clientId: '?client_id=', 
    redirect: '&redirect_uri=',
    scope: '&scope=',
    join: '%20',
    responseShow: '&response_type=token&show_dialog=true'
}